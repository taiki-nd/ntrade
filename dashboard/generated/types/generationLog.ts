// Code generated by bee8; DO NOT EDIT.

// GenerationLogTriggerType mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type GenerationLogTriggerType = 'manual' | 'issue_fix' | 'setup' | 'ci';

// GenerationLogStatus mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type GenerationLogStatus = 'success' | 'failed';

// GenerationLog is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface GenerationLog {
  id: string;
  project_id: string;
  identity_id: string;
  issue_id?: string;
  trigger_type: GenerationLogTriggerType;
  status: GenerationLogStatus;
  lint_passed: boolean;
  error_code?: string;
  duration_ms: number;
  domain_count: number;
  route_count: number;
  output_bytes: number;
  token_count: number;
  created_at: string;
}

// PublicGenerationLog is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicGenerationLog {
  id: string;
  project_id: string;
  issue_id?: string;
  trigger_type: GenerationLogTriggerType;
  status: GenerationLogStatus;
  lint_passed: boolean;
  duration_ms: number;
  created_at: string;
}



