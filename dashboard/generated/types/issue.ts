// Code generated by bee8; DO NOT EDIT.

// IssueStatus mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type IssueStatus = 'open' | 'chatting' | 'fixing' | 'review_ready' | 'closed';

// IssuePriority mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type IssuePriority = 'low' | 'medium' | 'high' | 'urgent';

// Issue is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface Issue {
  id: string;
  project_id: string;
  created_by_id: string;
  title: string;
  description: string;
  status: IssueStatus;
  priority: IssuePriority;
  branch_name?: string;
  preview_url?: string;
  created_at: string;
  updated_at: string;
  closed_at?: string;
}

// PublicIssue is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicIssue {
  id: string;
  project_id: string;
  created_by_id: string;
  title: string;
  description: string;
  status: IssueStatus;
  priority: IssuePriority;
  preview_url?: string;
  created_at: string;
  updated_at: string;
  closed_at?: string;
}

// CreateIssueRequest is the POST body for createIssue.
// Fields are derived from the domain's `create:` request override (or
// field list) — optional entries carry `?` when the server decodes them
// as pointers.
export interface CreateIssueRequest {
  project_id: string;
  created_by_id: string;
  title: string;
  description: string;
  status: IssueStatus;
  priority: IssuePriority;
  branch_name: string;
  preview_url: string;
  closed_at: string;
}

// UpdateIssueRequest is the PUT body for updateIssue.
// All fields are optional — the server skips SET clauses for nil slots.
export interface UpdateIssueRequest {
  project_id?: string;
  created_by_id?: string;
  title?: string;
  description?: string;
  status?: IssueStatus;
  priority?: IssuePriority;
  branch_name?: string;
  preview_url?: string;
  closed_at?: string;
}

