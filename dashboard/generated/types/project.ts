// Code generated by bee8; DO NOT EDIT.

// ProjectDatabaseType mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type ProjectDatabaseType = 'postgres' | 'mysql' | 'sqlite';

// ProjectStatus mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type ProjectStatus = 'active' | 'archived';

// Project is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface Project {
  id: string;
  identity_id?: string;
  organization_id?: string;
  name: string;
  slug: string;
  database_type: ProjectDatabaseType;
  status: ProjectStatus;
  preview_url?: string;
  repo_url?: string;
  setup_prompt?: string;
  created_at: string;
  updated_at: string;
  deleted_at?: string;
}

// PublicProject is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicProject {
  id: string;
  name: string;
  slug: string;
  database_type: ProjectDatabaseType;
  status: ProjectStatus;
  preview_url?: string;
  repo_url?: string;
  created_at: string;
}

// CreateProjectRequest is the POST body for createProject.
// Fields are derived from the domain's `create:` request override (or
// field list) — optional entries carry `?` when the server decodes them
// as pointers.
export interface CreateProjectRequest {
  identity_id: string;
  organization_id: string;
  name: string;
  slug: string;
  database_type: ProjectDatabaseType;
  status: ProjectStatus;
  preview_url: string;
  repo_url: string;
  setup_prompt: string;
}

// UpdateProjectRequest is the PUT body for updateProject.
// All fields are optional — the server skips SET clauses for nil slots.
export interface UpdateProjectRequest {
  identity_id?: string;
  organization_id?: string;
  name?: string;
  slug?: string;
  database_type?: ProjectDatabaseType;
  status?: ProjectStatus;
  preview_url?: string;
  repo_url?: string;
  setup_prompt?: string;
}

