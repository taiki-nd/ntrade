// Code generated by bee8; DO NOT EDIT.

// User is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface User {
  id: string;
  name: string;
  avatar_url?: string;
  created_at: string;
  updated_at: string;
}

// PublicUser is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicUser {
  id: string;
  name: string;
  avatar_url?: string;
  created_at: string;
}


// UpdateUserRequest is the PUT body for updateUser.
// All fields are optional — the server skips SET clauses for nil slots.
export interface UpdateUserRequest {
  name?: string;
  avatar_url?: string;
}

