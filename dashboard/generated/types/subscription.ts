// Code generated by bee8; DO NOT EDIT.

// SubscriptionPlan mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type SubscriptionPlan = 'free' | 'pro' | 'team' | 'enterprise';

// SubscriptionStatus mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type SubscriptionStatus = 'active' | 'past_due' | 'canceled' | 'trialing';

// Subscription is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface Subscription {
  id: string;
  identity_id?: string;
  organization_id?: string;
  stripe_customer_id: string;
  stripe_subscription_id?: string;
  plan: SubscriptionPlan;
  status: SubscriptionStatus;
  current_period_end?: string;
  cancel_at_period_end: boolean;
  created_at: string;
  updated_at: string;
}

// PublicSubscription is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicSubscription {
  id: string;
  plan: SubscriptionPlan;
  status: SubscriptionStatus;
  current_period_end?: string;
  cancel_at_period_end: boolean;
}



