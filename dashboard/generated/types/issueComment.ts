// Code generated by bee8; DO NOT EDIT.

// IssueCommentSenderType mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type IssueCommentSenderType = 'user' | 'ai' | 'system';

// IssueComment is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface IssueComment {
  id: string;
  issue_id: string;
  sender_id?: string;
  sender_type: IssueCommentSenderType;
  content: string;
  metadata?: string;
  created_at: string;
}

// PublicIssueComment is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicIssueComment {
  id: string;
  issue_id: string;
  sender_id?: string;
  sender_type: IssueCommentSenderType;
  content: string;
  metadata?: string;
  created_at: string;
}

// CreateIssueCommentRequest is the POST body for createIssueComment.
// Fields are derived from the domain's `create:` request override (or
// field list) — optional entries carry `?` when the server decodes them
// as pointers.
export interface CreateIssueCommentRequest {
  issue_id: string;
  sender_id: string;
  sender_type: IssueCommentSenderType;
  content: string;
  metadata: string;
}


