// Code generated by bee8; DO NOT EDIT.

// LicenseKeyStatus mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type LicenseKeyStatus = 'active' | 'revoked' | 'expired';

// LicenseKey is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface LicenseKey {
  id: string;
  subscription_id: string;
  key_hash: string;
  key_prefix: string;
  status: LicenseKeyStatus;
  expires_at: string;
  last_verified_at?: string;
  created_at: string;
}

// PublicLicenseKey is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicLicenseKey {
  id: string;
  key_prefix: string;
  status: LicenseKeyStatus;
  expires_at: string;
  last_verified_at?: string;
  created_at: string;
}



