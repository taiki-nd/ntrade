// Code generated by bee8; DO NOT EDIT.

// IdentityRole mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type IdentityRole = 'user' | 'admin';

// Identity is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface Identity {
  id: string;
  email: string;
  role: IdentityRole;
  user_id?: string;
  admin_id?: string;
  last_login_at?: string;
  created_at: string;
}

// PublicIdentity is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicIdentity {
  id: string;
  email: string;
  role: IdentityRole;
  last_login_at?: string;
}


// UpdateIdentityRequest is the PUT body for updateIdentity.
// All fields are optional — the server skips SET clauses for nil slots.
export interface UpdateIdentityRequest {
  email?: string;
  role?: IdentityRole;
  user_id?: string;
  admin_id?: string;
  last_login_at?: string;
}

