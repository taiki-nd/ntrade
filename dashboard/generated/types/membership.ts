// Code generated by bee8; DO NOT EDIT.

// MembershipRole mirrors the `values:` list on the schema — emitted as a
// string-literal union so narrowing + JSON round-trips work without an
// intermediate enum wrapper.
export type MembershipRole = 'owner' | 'member' | 'client';

// Membership is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface Membership {
  id: string;
  organization_id: string;
  identity_id: string;
  role: MembershipRole;
  created_at: string;
}

// PublicMembership is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicMembership {
  id: string;
  organization_id: string;
  identity_id: string;
  role: MembershipRole;
  created_at: string;
}

// CreateMembershipRequest is the POST body for createMembership.
// Fields are derived from the domain's `create:` request override (or
// field list) — optional entries carry `?` when the server decodes them
// as pointers.
export interface CreateMembershipRequest {
  organization_id: string;
  identity_id: string;
  role: MembershipRole;
}

// UpdateMembershipRequest is the PUT body for updateMembership.
// All fields are optional — the server skips SET clauses for nil slots.
export interface UpdateMembershipRequest {
  organization_id?: string;
  identity_id?: string;
  role?: MembershipRole;
}

