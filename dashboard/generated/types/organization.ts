// Code generated by bee8; DO NOT EDIT.

// Organization is the DB row shape. Sensitive columns (e.g. password
// hashes) are elided because they never leave the server — the DTOs
// below are what endpoints actually return.
export interface Organization {
  id: string;
  name: string;
  slug: string;
  created_at: string;
  updated_at: string;
}

// PublicOrganization is the public-variant DTO. Ref entries are
// inlined — the server resolves each ref to its corresponding public
// DTO before serialising.
export interface PublicOrganization {
  id: string;
  name: string;
  slug: string;
  created_at: string;
}

// CreateOrganizationRequest is the POST body for createOrganization.
// Fields are derived from the domain's `create:` request override (or
// field list) — optional entries carry `?` when the server decodes them
// as pointers.
export interface CreateOrganizationRequest {
  name: string;
  slug: string;
}

// UpdateOrganizationRequest is the PUT body for updateOrganization.
// All fields are optional — the server skips SET clauses for nil slots.
export interface UpdateOrganizationRequest {
  name?: string;
  slug?: string;
}

