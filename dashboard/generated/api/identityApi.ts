// Code generated by bee8; DO NOT EDIT.
import { apiFetch, parseJSON } from './client';
import type {
  DataResponse,
} from '../types/common';

import type { PublicIdentity, UpdateIdentityRequest } from '../types/identity';

// BASE_URL is the path prefix for every call in this file. Configured via
// project.yaml's `typescript.base_url` — change it there, not here.
const BASE_URL = '/api/v1';


// getIdentity issues GET /identities/{id}. Throws ErrorResponse on 4xx/5xx.
export async function getIdentity(id: string): Promise<DataResponse<PublicIdentity>> {
  const res = await apiFetch(`${BASE_URL}/identities/${id}`);
  return parseJSON<DataResponse<PublicIdentity>>(res);
}

// updateIdentity issues PUT /identities/{id}.
export async function updateIdentity(id: string, req: UpdateIdentityRequest): Promise<DataResponse<PublicIdentity>> {
  const res = await apiFetch(`${BASE_URL}/identities/${id}`, {
    method: 'PUT',
    body: JSON.stringify(req),
  });
  return parseJSON<DataResponse<PublicIdentity>>(res);
}

