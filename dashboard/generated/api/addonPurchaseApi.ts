// Code generated by bee8; DO NOT EDIT.
import { apiFetch, parseJSON } from './client';
import type {
  DataResponse,
  CursorListResponse,
  OffsetListResponse,
} from '../types/common';

import type { PublicAddonPurchase } from '../types/addonPurchase';

// BASE_URL is the path prefix for every call in this file. Configured via
// project.yaml's `typescript.base_url` — change it there, not here.
const BASE_URL = '/api/v1';


// listAddonPurchases issues GET addon_purchases. Paginate mode: none (full array in `data`).
export async function listAddonPurchases(params?: {
  per_page?: number;
}): Promise<DataResponse<PublicAddonPurchase[]>> {
  const query = new URLSearchParams();
  if (params?.per_page !== undefined) query.set('per_page', String(params.per_page));
  const qs = query.toString();
  const url = `${BASE_URL}/addon_purchases${qs ? `?${qs}` : ''}`;
  const res = await apiFetch(url);
  return parseJSON<DataResponse<PublicAddonPurchase[]>>(res);
}

// getAddonPurchase issues GET /addon_purchases/{id}. Throws ErrorResponse on 4xx/5xx.
export async function getAddonPurchase(id: string): Promise<DataResponse<PublicAddonPurchase>> {
  const res = await apiFetch(`${BASE_URL}/addon_purchases/${id}`);
  return parseJSON<DataResponse<PublicAddonPurchase>>(res);
}

