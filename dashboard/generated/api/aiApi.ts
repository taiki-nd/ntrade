// Code generated by bee8; DO NOT EDIT.
import { apiFetch } from './client';

const BASE_URL = '/api/v1';

// Endpoints-routes have no typed Request/Response contract (their
// handlers are hand-scaffolded on the server). The generated functions
// below return the raw Response so callers can shape the payload with
// whatever typing they need — webhooks, health checks, and third-party
// callbacks routinely don't follow the envelope.

// setupChatAiProjects issues POST  for route endpoint `SetupChatAiProjects`.
export async function setupChatAiProjects(init?: RequestInit
): Promise<Response> {
  return apiFetch(`${BASE_URL}/projects/ai/setup-chat`, { method: 'POST', ...init });
}

// chatIssues issues POST (with {id}) for route endpoint `ChatIssues`.
export async function chatIssues(id: string, init?: RequestInit
): Promise<Response> {
  return apiFetch(`${BASE_URL}/issues/${id}/chat`, { method: 'POST', ...init });
}

// executeFixIssues issues POST (with {id}) for route endpoint `ExecuteFixIssues`.
export async function executeFixIssues(id: string, init?: RequestInit
): Promise<Response> {
  return apiFetch(`${BASE_URL}/issues/${id}/execute-fix`, { method: 'POST', ...init });
}

