// Code generated by bee8; DO NOT EDIT.
import { apiFetch } from './client';

const BASE_URL = '/api/v1';

// Endpoints-routes have no typed Request/Response contract (their
// handlers are hand-scaffolded on the server). The generated functions
// below return the raw Response so callers can shape the payload with
// whatever typing they need — webhooks, health checks, and third-party
// callbacks routinely don't follow the envelope.

// stripeWebhooks issues POST  for route endpoint `StripeWebhooks`.
export async function stripeWebhooks(init?: RequestInit
): Promise<Response> {
  return apiFetch(`${BASE_URL}/webhooks/stripe`, { method: 'POST', ...init });
}

// checkoutSessionsBilling issues POST  for route endpoint `CheckoutSessionsBilling`.
export async function checkoutSessionsBilling(init?: RequestInit
): Promise<Response> {
  return apiFetch(`${BASE_URL}/billing/checkout-sessions`, { method: 'POST', ...init });
}

// portalSessionsBilling issues POST  for route endpoint `PortalSessionsBilling`.
export async function portalSessionsBilling(init?: RequestInit
): Promise<Response> {
  return apiFetch(`${BASE_URL}/billing/portal-sessions`, { method: 'POST', ...init });
}

