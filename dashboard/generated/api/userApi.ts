// Code generated by bee8; DO NOT EDIT.
import { apiFetch, parseJSON } from './client';
import type {
  DataResponse,
} from '../types/common';

import type { PublicUser, UpdateUserRequest } from '../types/user';

// BASE_URL is the path prefix for every call in this file. Configured via
// project.yaml's `typescript.base_url` — change it there, not here.
const BASE_URL = '/api/v1';


// getUser issues GET /users/{id}. Throws ErrorResponse on 4xx/5xx.
export async function getUser(id: string): Promise<DataResponse<PublicUser>> {
  const res = await apiFetch(`${BASE_URL}/users/${id}`);
  return parseJSON<DataResponse<PublicUser>>(res);
}

// updateUser issues PUT /users/{id}.
export async function updateUser(id: string, req: UpdateUserRequest): Promise<DataResponse<PublicUser>> {
  const res = await apiFetch(`${BASE_URL}/users/${id}`, {
    method: 'PUT',
    body: JSON.stringify(req),
  });
  return parseJSON<DataResponse<PublicUser>>(res);
}

